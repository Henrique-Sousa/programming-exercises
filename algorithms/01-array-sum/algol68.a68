PROC sum_array = (REF []INT a, INT n) INT:
(
    INT sum := 0;
    FOR i FROM 1 TO n DO
        sum := sum + a[i]
    OD;
    sum
);

INT size = 6;
[1:6]INT arr := (1, 2, 3, 4, 10, 11);

print((sum_array(arr, size), newline))
